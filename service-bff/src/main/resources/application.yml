server:
  port: 8080

app:
  kafka:
    bootstrap-servers: localhost:29092
    messages-topic: ad-manager-events

spring:
  kafka:
    bootstrap-servers: ${app.kafka.bootstrap-servers}
  application:
    name: service-bff
